from __future__ import annotations


def structured_extract(text: str, section: str) -> dict:
    # Deterministic placeholder for structured extraction; replace with real LLM adapter.
    return {"section": section, "raw_text": text[:2000]}
